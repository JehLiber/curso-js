let num1 = Number(prompt('Digite um número:'));
let num2 = Number(prompt('Digite outro número:'));

const resultado = num1 + num2;

alert(`O resultado da sua conta foi: ${resultado}`); /* ${} é muito utilizado pela praticidade para resolver os resultados de forma prática, por exemplo ${num1 + num2} diretamente no resultado, porém em códigos mais complexos pode ficar confuso, ter boa prática em um código é muito importante para bom entendimento*/ 